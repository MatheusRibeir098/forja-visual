# pnpm >= 11 lê as settings daqui (o campo "pnpm" do package.json foi descontinuado).
packages:
  - .

# Pré-aprova os build scripts nativos — evita o prompt interativo de `pnpm approve-builds`.
allowBuilds:
  esbuild: true

onlyBuiltDependencies:
  - esbuild
