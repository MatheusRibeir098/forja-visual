# Progresso — Forja Visual · Protótipo 01

## Estado
- Rodada 1 iniciada em 2026-08-24. Spec aprovada pelo dono (prompt.md).
- Decisões: vanilla TS + three core (OGL plano B), sem GSAP/Lenis/Tailwind, relevo tipográfico "FORJA" como asset.
- Divergência L2: 3 variantes A/B/C → dono escolhe por 1 print cada.
- Tentativas: — (nenhuma tarefa repetida até aqui)
- ⏸ 2026-08-24: sessão encerrada no meio do T13 (dev morto antes de escrever `medicao/index.ts`). Ver `HANDOFF.md`.
- ▶ 2026-08-25: sessão retomada. L3 + T16 fechados e marcados. L4 em execução: T13 e T14 disparados **em paralelo** (arquivos disjuntos — T13 dono de `src/sections/medicao/*`, T14 dono de `src/styles/*` + os `style.css` das outras 7 seções).
- ✅ **Decisão do dono (2026-08-25): F2 mantém específico → média.** Argumento aceito: o hero já entrega a tela no específico, então a transição roda na mesma cena, sem corte, e termina no parágrafo "é para lá que tudo tende sozinho". A ordem da spec §3 abriria com emenda e repetiria o gesto do hero. **Assunto encerrado** — não reabrir.
- Pendências do T13 já resolvidas pelo dev interrompido (conferidas no repo): `campo` no `index.html` e em `site.sections`; `content/index.ts` re-exportando `campo`; `e2e/validate-l2.ts` removido; `main.ts` completo com a regra do canvas escrita em comentário.

- 🔓 **Diretriz do dono (2026-08-25): teto de bytes suspenso.** A prioridade passa a ser qualidade visual e impacto — não economia de KB. Os orçamentos de 300 KB (crítico) e 600 KB (lazy) deixam de reprovar uma entrega; sigam sendo **medidos e relatados**, como número informativo. **Continuam valendo como aceite** (são qualidade, não custo): 60 FPS em GPU real, contraste ≥ 7:1, WCAG 2.2 AA, `prefers-reduced-motion`, 1 único rAF e a regra do canvas do `main.ts`.

## Ciclos
### Rodada de acabamento (pedidos do dono olhando o site pronto, 2026-08-25)
- **T28 (1ª)** dev OK — **a piada da abertura agora se lê.** O dono viu a caricatura do site-médio-de-IA (gradiente roxo, cards de vidro, dois botões) e perguntou *"que fundo azul com cards é esse?"* — achou que fosse defeito. O efeito dependia de reconhecimento instantâneo e falhava até com quem conhece o projeto. Mensagem revelada ~0,25 s após o fim da varredura: *"Por meio segundo, você achou que era mais um."* + explicação de que a máquina entrega a média de tudo que viu. Sob reduced-motion aparece junto com o resto, sem animação. Contraste inalterado (7,93).
- **T29 (1ª)** dev OK — crânio dá **volta completa** e volta a encarar o visitante (`SCROLL_TURNS` 0,55 → 1). Elegância do fecho: `BASE_YAW + TAU` é o mesmo ângulo visual por periodicidade, então **não precisa de caso especial** para não saltar. Enquadramento e prepass já eram calculados independentes de pose — `scene.ts` intocado. Verificado por screenshot em 0/25/50/75/100% do percurso: pose inicial e final idênticas, sem toque nas bordas nem na vista de costas.
- **T30 (1ª)** dev OK — **corte na coluna PESO do catálogo: bug antigo, não da virada de paleta.** `mountCatalog` põe `.cat` **no mesmo elemento** que já é `.section`, empilhando `padding-inline: var(--gutter)` (base) com `max-inline-size` + `margin-inline: auto` (catálogo). A grade de 12 col ficava em 1151,8 px em vez de 1248 — **~7% mais estreita**, empurrando PESO para dentro. Só se manifesta acima de ~84rem (content-max + 2 gutters), por isso passou despercebido. Corrigido com `padding-inline: 0` + `max-inline-size: min(var(--content-max), 100% - 2*var(--gutter))`. Checador da seção: **desvio máximo 0,0000 px em 147 amostras**.
  ⚠️ A pendência dele especula "scroll inercial do **Lenis**" — **o projeto não usa Lenis** (banido pelo P5; a única ocorrência no repo é uma URL de artigo em `content/tecnicas.ts`). Hipótese descartada; se o corte reaparecer, investigar a faixa de foco de leitura (`focus.ts`/`planes.ts`) sem essa premissa.
- **T31 (1ª)** dev OK — **tabela de medição desalinhada: `display: grid` aplicado direto no `<th>`/`<td>`.** O computed display deixava de ser `table-cell` e a célula empilhava como bloco solto abaixo da anterior, em vez de ocupar sua coluna (confirmado por `getComputedStyle` antes/depois). Corrigido movendo o grid para um `<div>` filho, mantendo a célula como `table-cell` puro. Junto: as 3 linhas de bytes viraram `enforced: false` — `ok` sempre `null`, régua satura em 100% com selo textual do excedente (**341%**) e **listra pontilhada**, distinta da listra diagonal de falha real; FPS e contraste seguem `enforced: true`. De brinde, achou overflow real em 375 px: a string do driver no "Renderizador" é token longo sem espaço e vazava sobre a coluna vizinha (`overflow-wrap: anywhere`).
- **T32 (1ª)** dev OK — texto acompanhou o comportamento: coluna **"Teto" → "Referência"**, caption agora *"Referências da spec §6 confrontadas com o medido — bytes informam, contraste e FPS reprovam."*, e `status.ok/over` trocaram "orçamento" por "critério". Estrutura de `MedicaoCopy` intacta (só valores), 65 testes verdes.
- 🏁 **Validação final 10:38:** typecheck · lint · **65 testes** · build verdes. `minContrast` **7,93:1**. FPS mediana **59,9** nos dois tiers. Bytes: lazy 2043,0 KB (referência 600, informativo).
- **T27 (2 tentativas) — a cauda de p5 NÃO era o bloom.** Dois devs cortaram bloom em sequência (níveis 3→2, primeiro estágio em meia resolução, depois bloom **inline** com 8 taps e zero `setRenderTarget`) e a cauda sobreviveu a tudo. O 2º refutou a própria hipótese do briefing — stall de driver por troca de render target — e, ao aplicar o fallback (`bloomLevels: 0`), mediu **p5 29,9 com o bloom desligado**. Foi o que entregou o caso.
  **Diagnóstico do orquestrador:** medi 3× com bloom off → p5 30,0 / 30,1 / 30,0, com GPU mediana 11,2 e p95 dentro do teto. Com bloom on, o dev tinha medido 59,5 / 59,5 / 30,1. **Não correlaciona com o bloom e não falta GPU.** `ps` mostrou a causa: **Spotify a 48,6% de CPU com gpu-process próprio + Chrome pessoal com ~30 processos** disputando a mesma Intel integrada. O que cai é o navegador perdendo vsync por contenção — a máquina de medição **nunca esteve isolada** em nenhuma medição da sessão (o dev do T19 já tinha suspeitado disso e sido ignorado).
  **Bloom religado** (`bloomLevels: 1` no high) com o diagnóstico documentado no `tier.ts` — senão o próximo dev que olhar o p5 corta o efeito de novo. Processos do dono **não foram mortos** (`safe-operations`).
  ⚠️ **Lição de método, não deste projeto:** o aceite era "mediana ≥ 60 fps" e a mediana passou o tempo todo. Quando o T18 acrescentou p5/p95, o número novo foi tratado como **veredito** sem que ninguém validasse o **ambiente** de medição — e dois devs gastaram ~20 min cada perseguindo ruído. Regra a destilar: *medida nova exige validação do ambiente antes de virar critério; se um número não correlaciona com a variável que você mexe, o problema não é a variável.*
- 🏁 **FECHAMENTO DA RODADA (10:07, prazo 10:15 cumprido).** typecheck · lint · **65 testes** · build verdes. `minContrast` **7,93:1** (piso 7). FPS mediana **59,9** nos dois tiers; GPU mediana **10,95 ms** (high) e 12,42 (low). Bytes informativos: crítico 176,5 · lazy 2043,0 KB. **T23 (cursor com memória) cortado por prazo**, como avisado no checkpoint — era o item de menor impacto do plano e o único não iniciado.
- **T21 (1ª) — ESTRUTURAL** dev OK — **a página passou a compor como um quadro só.** `src/engine/frame.ts` (FBO de página compartilhado) + `src/shaders/grade.ts` (curva fílmica → bloom em mip-chain de 4 taps, mão-feito → vinheta → grão 1:1 → dither por *Interleaved Gradient Noise*, aproximação de blue-noise sem textura). `hero/tese/campo/relevo/catalogo` escrevem no FBO; `main.ts` liga por duas inscrições no ticker (`beginFrame` primeiro, `present` por último) e o comentário da REGRA DO CANVAS foi reescrito. Nada de `postprocessing`/`EffectComposer` — tudo à mão, como a §7 exige.
  🔧 **Descoberta que mudou o plano:** `renderer.setViewport/setScissor()` são **ignorados** quando `composite.ts` troca de render target por baixo — o three reaplica o scissor do próprio `WebGLRenderTarget`, não o solto no renderer. Daí hero/tese usarem `gl.frame.setScissorCss()` (grava em `target.scissor`) enquanto campo/relevo/catálogo, que nunca trocam de target, seguem com `renderer.setScissor()`.
  🔧 **Desvio deliberado e medido: RGBA16F → RGBA8** no FBO e nos mips. 16F custava 13,72 ms de mediana no high (teto ~13,5); RGBA8 caiu para 9,8–12,05. Argumento: as 11 técnicas já terminam com `linearToSrgb` — o FBO nunca teve mais que 8 bits reais; **quem mata banding é o dither, não a precisão do buffer**. `bloomLevels` 3/2/0 por tier, mesmo padrão do `rayMarchSamples`.
- ✅ **Medição conjunta (T24), máquina isolada, 09:40:** typecheck · lint · **65 testes** · build **verdes**. `minContrast` **7,93:1** (piso 7; pior caso `p.relevo__limit`). Bytes informativos: crítico 176,6 · lazy 2043,0 · total 2269,6 KB.
- 🔴 **Regressão encontrada na cauda, não na mediana:** tier high deu **p5 30,0 fps** em 2 de 3 execuções isoladas, com GPU p95 17,2–17,5 ms (folga **negativa**). Mediana cabe (11–12 ms), a cauda estoura. Antes do passe, o p5 era 59,88 consistente. **O critério de aceite (mediana) estava sendo generoso demais** — só o p5/p95 do medidor novo (T18) revelou. T27 disparada para recuperar margem com alvo de **três execuções consecutivas** estáveis, não uma run sortuda.
- **T26 (1ª)** dev OK — campo sai da ilha: `--campo-ink` #f2efe9 → **#f0efec** (agora idêntico ao `--fg` global), `--campo-ink-dim` #b0aca4 → **#c8c4bc** (um degrau acima do `--muted`, exigido pelo mono pequeno do rodapé de figura, que é o pior caso pixel-a-pixel), `--campo-ember` #ff9d5c → **#ffb37a**. Tudo por **cor sólida, não alpha**. `--campo-void`/`--campo-plate`/`--campo-rule` intocados de propósito: o primeiro é contrato com o renderer no `index.ts`, que não era dele.
  `dd.campo__figure-label` **6,86 → 8,88:1**. Crédito CC BY (`a.campo__link`) a **16,06:1**. 375×667 sem overflow (`scrollWidth === clientWidth === 375`).
  → **`minContrast` global 7,93:1, acima do piso.** Novo pior caso `p.relevo__limit` — já dentro da faixa aprovada. A cadeia de contraste está **verde pela primeira vez desde o início da sessão**; falta remedir depois do grade.
- **T22 (1ª)** dev OK — **paleta grafite comitada**: `tokens.css` passa a declarar `--bg #101010 / --fg #f0efec / --muted #bcbab6 / --accent #a8a8a8 / --rule #333230` como default único, **sem depender de `prefers-color-scheme`**. Achado que encurtou a tarefa: `referencia`, `principios` e `medicao` **não precisaram de edição** — só consumiam tokens, então herdaram o escuro de graça; a disciplina de token dos devs anteriores pagou aqui. Só o `catalogo` tinha `--cat-*` próprios sob `@media dark`: os valores subiram para o default e a media query saiu. Grade de 12 col, numeração, fio e nota de margem intocados nos 4 arquivos.
  `p.pr-id` **6,53 → 8,40:1** (folga de 1,4 sobre o piso). O dev mediu com cópia read-only e escopada do script oficial, sem escrever em `src/generated` nem em `.forge/screenshots` — não sujou o estado de ninguém.
  → Novo pior caso global: **`dd.campo__figure-label` a 6,86:1**. Não é regressão: `campo` usa `--campo-*` locais, desacoplados do `tokens.css`, e ficou como ilha quando o resto virou grafite. Tarefa T26 disparada.
- **T25 (1ª)** dev OK — `measure-bundle.ts` deixa de reprovar por bytes: `*_BUDGET_KB` → `*_REFERENCE_KB`, impressos ao lado do medido. Guardas de **erro real** intactas (`dist/` ausente, gzip/leitura falha). `package.json` não precisou mudar. Provou que a barreira caiu: `pnpm measure` agora atravessa o bundle (`lazyKb 2043,02 KB · referência 600`) e chega ao contraste — que reprova por conta própria (6,53 < 7, o `p.pr-id` que está com a T22). Era exatamente o sintoma a eliminar: o portão que **deixou** de ser critério estava impedindo os dois que **continuam** sendo.
- **T17 (1ª)** dev OK — contraste do hero resolvido, com **dois** achados. (a) A máscara de threshold destrava A/B **por posição de tela, sem saber onde o texto HTML está**: durante a varredura, um pixel sob o bloco de texto podia continuar mostrando a camada A ("a média", brilhante) com o texto já revelado — ele capturou frames com glifos legíveis colados num blob azul. Fix: a camada A passa a receber a **mesma área segura** da B (`sceneAverage.setSafeArea`, mesmos `SAFE_MARGIN_NDC`/`SAFE_FEATHER_NDC`) e escurece **a cor, não o alfa**, com força por `uProgress` (zero durante o hold de 0,6 s, total antes da 1ª janela de texto) — a mistura fica escura **não importa qual lado o threshold escolha**. (b) O 2,86:1 relatado vinha de medir `.hero-goal` com `--hero-reveal` **exatamente 0**: 0 glifos desenhados, `clip-path` fechado — invisível ao olho e **invisível aos checks de opacity/visibility do medidor**, que classificava ruído de fundo como texto. Corrigido com `opacity: min(var(--hero-reveal,0)*1000, 1)`, sem mudança visual.
  Verificou a **faixa inteira** (frames a cada ~25–100 ms de 0→1), não só o instante do medidor. FPS 59,9 nos dois tiers.
  → `minContrast` global agora **6,53:1**, e o pior caso **mudou de dono**: é `p.pr-id` em `principios`, defeito **pré-existente** que estava mascarado atrás do 2,86 do hero. Foi para o briefing da T22.
- 🔍 **Causa do incidente identificada:** foi `git stash push` deste mesmo dev (stash faz reset interno — daí o `reset: moving to HEAD` no reflog), usado duas vezes para contornar erro de build alheio; na 2ª ele esqueceu o `pop`, percebeu sozinho, comparou byte a byte e resolveu com `drop`. **Reportou espontaneamente**, junto com um `rm -f scratchpad-verify.mjs` (não rastreado, não dele) feito por engano — esse arquivo está perdido. Os `scripts/tmp-measure-*.ts` que também sumiram eram temporários do dev do crânio, que os removeu de propósito. **Nada de valor se perdeu.**
- **T19 (1ª)** dev OK — relevo **1280×720 → 3200×1800** nativo. `BEVEL_BLUR_RADIUS_PX` 8→20 e `FINAL_BLUR_RADIUS_PX` 1→3 escalam 2,5× junto para manter a **mesma largura física** do bisel: o ganho vem de amostrar a mesma geometria com mais texels, não de mexer no ângulo de 45° que o `relight.ts` já tinha calibrado. `forja-grain.png` fica em 256×256 de propósito (o tamanho na tela é função de `uGrainTiles`, não da resolução do tile) — documentado no arquivo.
  **Truque extra da IV.1 implementado** (nunca tinha sido): segundo gradiente do **brilho do albedo** (Rec.709), amostrado a **8 texels** de distância — não ±1, porque a variação de desgaste é lenta demais e a ±1 leria só ruído de quantização de 8 bits. Fundido na normal via `uAlbedoReliefStrength` (1,5, medido).
  **Como provou que aparece na imagem** (e não só no código): script offline decodificou os PNG/WebP reais e replicou a matemática do shader em 20k pontos de **chapa lisa**, fora da palavra. Gradiente do depth ali é **exatamente zero** (chapa plana); o gradiente do albedo tem mediana 0,030 / p95 0,080 → ~2,5° de inclinação média e ~7° no p95, mesma ordem dos ~9° do grão. Sinal real, sem dominar o bisel.
  FPS mediana 59,9 (3 execuções). Determinismo sha256 confirmado. Assets: depth 1365,6 KB + albedo 19,1 + grão 54,7 (informativo).
- 🚨 **INCIDENTE — `git reset` de agente apagou trabalho não commitado.** O reflog registra `reset: moving to HEAD`; o dev do T19 perdeu as edições de `src/shaders/relight.ts` e `src/variants/b/relightPlane.ts` no meio da tarefa e teve de reaplicá-las. Nenhum briefing autorizava comando de restauração.
  **Ações tomadas:** backup do worktree inteiro fora do repo antes de continuar; aviso explícito enviado ao dev ainda em execução proibindo `reset`/`checkout --`/`stash`/`clean`/commit.
  **Lição para o Forge (fora deste projeto):** o hook `PreToolUse` bloqueia por caminho de escrita, mas **não bloqueia comando git destrutivo**. Um `git reset --hard` de um dev derruba o trabalho de todos os irmãos do lote e o hook não vê. Candidato a regra dura em `safe-operations` + hook: **dev nunca restaura arquivo por git; commit é só do orquestrador.**
- **T20 (1ª)** dev OK — crânio **12k → 45.000 pontos**, oclusor 4,2k → **8.000 tri**. Nada chutado: `SIZE_PER_RADIUS` 11,4 → **5,7** por varredura medindo saturação de pixel (canal ≥250/255, mesma técnica do `measure-contrast`) — 0,113% desktop / 0,128% mobile, batendo o alvo de ~0,12%. Descarte do prepass **53,7%** (51,0–56,5% na coreografia). `HULL_SHRINK_MARGIN` re-varrido: o mínimo segue **0,018**, mas o casco de 8k erra menos e o encolhimento caiu de 4,0% → **3,2%** do raio; o piso de auto-oclusão real caiu de 19,6% → **18,8%**. FPS 59,9/59,9 sem regressão (folga de GPU 3,27 ms high, 1,91 low). `TIER_POINT_FRACTION` não precisou mudar. Determinismo confirmado (sha256 idêntico). Assets 673,0 KB (informativo). Crédito CC BY intacto.
- **T18 (1ª)** dev OK — `measure-fps` agora mede **ms de GPU por quadro** (`EXT_disjoint_timer_query_webgl2`, ring de 4 queries, leitura atrasada, descarte correto de `GPU_DISJOINT_EXT`, dreno de 12 quadros no fim). Contexto obtido por segundo `getContext('webgl2')` sobre o canvas do three (mesma instância por spec) — 0 toque em `engine/`. `FpsMeasurement` ganhou 5 campos **opcionais**.
  🔴 **O número que mudava o plano: a folga é pequena.** high (1280×720@2x): mediana **10,54 ms**, p95 **13,77** → folga **2,90 ms**. low (375×667@1x): mediana **14,21**, p95 **15,38** → folga **1,29 ms**. 0 descartes por disjoint.
  ⚠️ Leitura minha: o low tem **15× menos pixels** que o high e custa **mais** — logo o gargalo do low **não é fill**, é overhead de geometria/draw call. Consequência para a T21: o passe de tela cheia escala com pixels, então é ~15× mais barato no low em termos absolutos; quem aperta lá é outra coisa. O teto real a respeitar é o **2,90 ms do high**.
- **T14 (1ª)** dev OK — auditoria responsiva/a11y dos 8 CSS. As seções já estavam bem construídas: 0 overflow em 375, `clamp()` na tipografia, grid do catálogo colapsa <56rem, `principios` já respeitava reduced-motion. Único defeito real: `:focus-visible` invisível em `hero` e `campo` (fundo sempre escuro + outline herdando `--fg` escuro no tema claro) — corrigido replicando o padrão da `tese`. Achou de quebra o bug do `vite.config.ts`.
- **Fix `vite.config.ts` (1ª)** dev OK — `forja-inline-principios` só exige o marcador quando `ctx.filename` bate com o `index.html` real; `dev/*.html` passam intactas. As 5 páginas de dev voltaram a 200, `/` segue com F6 inlinada, e a guarda de falha alta foi testada nos dois sentidos (dev 500 + build com mensagem clara) com o `index.html` restaurado depois.
- 🎯 **Curadoria da passada de excelência (2026-08-25)** — diagnóstico do "por que está tímido": não é falta de técnica, são **três decisões tomadas sob orçamento**. (a) a regra do scissor particiona a tela, então cada momento 3D é uma caixa com borda e a imagem nunca é composta como um quadro só; (b) zero pós-processamento por proibição de spec — sRGB cru, sem grão/vinheta/bloom/dither; (c) os dois assets em meia resolução (relevo 1280×720 rejeitado do 1600×900 pelo teto; crânio 12k contra os 48k da referência), com o motivo escrito nos dois scripts. Some a alternância papel-claro ↔ chapa-preta 5× em 8 seções: falta **uma atmosfera**.
  Plano em 5 (T19–T23), ordenado por impacto ÷ risco; a estrutural é a **T21**. Reprovados mesmo com teto solto: `postprocessing`/`EffectComposer`/`drei` (§7 + VI.7 aloca RT ao existir), GSAP/Lenis (P5 é regra de produto), WebGPU/TSL (invalidaria 1.800 linhas de GLSL por ganho que não é o gargalo — p5 já é 59,88), `antialias:true` (imagem final vem de quad, MSAA do backbuffer é descartado), `rayMarchSamples` > 8 (6× o custo pela mesma imagem), mais limalha no hero (26k é limite de legibilidade, não de bytes), II.1/II.3/III.3 (cobertura de catálogo por cobertura = P4 ao contrário), e preloader (o hero **é** a sequência de entrada).
  Números da base: crítico 172,8 · lazy 500,8 · total 723,6 KB; FPS high mediana **59,88 com p5 59,88** — zero quadros perdidos, mas **margem desconhecida**, daí a T18 antes da T21.
- **T13 (2ª — retomada)** dev OK — `medicao/index.ts` + `style.css`. Ficha monta com `buildSheet`+`buildMedicao`; estáticos de `@/generated`, runtime do engine (tier/dpr/renderer/reducedMotion/fps do ticker), `applyRow` a ~1×/s no ticker existente (0 rAF novo). Sob reduced-motion o fps é `null` e não há quadro extra. Tabela `table-layout: fixed`, 0 scroll horizontal em 375; régua de orçamento por **listra**, não por cor; status também em `.me__sr`. typecheck/lint/test/build verdes.
  **Orçamento medido: crítico 172,75/300 · lazy 500,82/600 KB** — three seguiu no caminho crítico, como previsto.
  ⚠️ `pnpm measure` abortou no `measure-contrast`, **fora da fronteira dele**: `p.hero-goal` a **2,86:1** (piso 7:1; era 17,19:1 na medição anterior) → regressão em hero/`src/styles`, que é justamente o território do T14 rodando em paralelo. `measure-fps` nem chegou a rodar (a cadeia aborta no primeiro erro).
  ⚠️ `scripts/_tmp-check-overflow.ts` e `scripts/_tmp-shots.ts` (soltos, de outro agente) quebram o `typecheck` do repo inteiro com TS5097 (import com extensão `.ts`). Remover ou corrigir antes do T15.
- **T4 (1ª)** OK — 8 arquivos em `src/content/` (16 técnicas, 5 fatores, P1–P7, 9 regras, roadmap, site). Typecheck strict verde. Nota p/ UI: `Technique.source` é opcional e nas V.x é caminho, não URL.
- **T1 (1ª)** OK — Vite 8 + TS 6.0.3 (TS7 incompatível c/ typescript-eslint) + three 0.185 em chunk próprio. Fontes: Instrument Serif + Instrument Sans var (49,9 KB). dist 3 KB gzip. Pendências: happy-dom p/ Vitest (T3 instala); CLI playwright ausente → e2e via `tsx e2e/run.ts`.
- **T3 (1ª)** OK — beats/damp/pointer + 21 testes (happy-dom). ⚠️ GLSL cursor-raio correto é `mv.xy + ray * mv.z` (soma; catálogo V.4 tem o sinal errado). Damp: critério ajustado p/ 10% do gap em ≤0,35 s (250 ms medido; 1% seria 800 ms). Pendência: `vitest.config.ts` fora do include do tsconfig.node.
- **T5 (1ª)** OK — measure-{bundle,contrast,fps} + build-relief. GPU real: Intel RPL-U via `--use-gl=angle --use-angle=gl` (headless puro = SwiftShader). Relevo: depth 250 KB @1280×720 (16-bit R+G), albedo 5,6 KB, grão em tile separado `forja-grain.png` (contrato em public/relief/README.md). Contraste placeholders 17:1; FPS 59,88 (tolerância vsync 1 frame).
- **T2 (1ª)** OK — ticker/tier/gl/composite + thresholdMask; máscara equalizada por histograma, ≥30% A/B puro em 5 aspects; three 123,9 KB gzip. Pendências → T2b: beats.ts fallback rAF viola "1 rAF"; field.ts 2 erros lint.
- **T2b (1ª)** OK — `createEngine()` em src/engine/index.ts; beats sem fallback rAF (+`onDirty` p/ reduced-motion demand); `dev` no include do tsconfig. field.ts já estava limpo. Demo check pass em GPU real.
- **T6 (1ª)** dev OK — variante A: caricatura da média em SDF → máscara 1,5 s → campo de ~11k limalhas magnéticas (cursor-raio). Contraste 11,35:1 (atenuar cor, não alpha — alpha converge p/ cor da fonte). 59,9 FPS. Pendência p/ L3: área escurecida atrás do texto precisa re-medir por scroll (beats). Aguarda tester.
- **T8 (1ª)** dev OK — variante C: editorial claro 12 col, domSync.ts (I.2, 8 testes, desvio ~1e-14 px), tinta com bleed/fora-de-registro atrás do título. Contraste 15,05:1, 59,88 FPS. Pendência: re-export de createDomSync em engine/index.ts; strings de mobília em markup.ts → content se vencer. Aguarda tester.
- **T7 (1ª)** dev OK — variante B: relight.ts (IV.1: 16-bit R+G nearest, normais por diferença central, ray march 8) + cursor-raio + damp na luz + órbita 9 s sem cursor. 59,9 FPS. ⚠️ tier.ts entrega rayMarchSamples 48/24/12 (não 8/4/0); B limita em 8 localmente — decidir em L3. Aguarda tester.
- **T9 (1ª)** dev OK — variante A promovida a `sections/hero/` (`va-*`→`hero-*`, `mountHero`→`mountSection`); área escurecida agora reconstruída por beat 'enter'→'exit' a cada quadro (0 getBoundingClientRect no loop). F2 `sections/tese/` com beat `top`→`bottom` cru em `composite.progress` (sem relógio; parar de rolar congela a máscara). 65 testes, build verde, 1 rAF. ⚠️ F2 rola do **específico → média** (spec §3 sugere o inverso) — decisão levada ao dono. Pendências p/ T13: regra única de "quem desenha o composite no quadro"; seções após F2 precisam de fundo opaco. Bug fora de escopo: `e2e/validate-l2.ts` tem MIN_CONTRAST/MIN_FPS não usados → quebra typecheck/lint do repo inteiro.
- **T10 (1ª)** dev OK — F3 `referencia/`: 5 beats (um por fator) + agulha DOM perseguindo a soma com damp; anel do alvo desenhado ao lado da agulha (a distância entre os dois É a técnica). Medido em GPU real, 3 runs: 10% do gap em 234–248 ms (teto 350), 0 trocas de sinal, beat imune a `prepend` de 800 px. F6 `principios/`: 17 animações, **todas ViewTimeline** + `@starting-style`, 0 elementos com style inline — zero JS de animação. 8 testes novos. ⚠️ Acoplamento não previsto: F3/F6 consomem `formatSectionMark` de `content/editorial.ts`, criado pelo T12 **em paralelo** — renomear quebra as duas. Pendência T13: F6 só é 100% JS-off se o markup for inlinado no index.html (`renderPrincipios()` já devolve a string pronta).
- **T12 (1ª)** dev OK — F5 `catalogo/`: índice editorial das 16 técnicas (5 grupos, 12 col, nota de margem), 1 plano WebGL por verbete + shader de tinta com bleed/domain warp no hover e cabeça de leitura no scroll. `domSync` ganhou `rectOf()`, `viewport`, `projectMeshToScreen`/`screenRectDelta` (projeção inversa = o aceite virou número); `createDomSync` re-exportado. Medido em GPU real: **desvio 2,3e-13 px** em 140 amostras/40 quadros, 59,88 fps, console limpo, 375×667 ok. Batch: `domSync.update()` segue sendo a ÚNICA leitura de layout do quadro (16 rects); a seção deriva scroll e faixa de scissor desses mesmos rects — 0 leitura extra. `variants/c/grain.ts` promovido a `sections/catalogo/grain.ts` (podar variants/ agora quebraria F5).
- ⚠️ **Convergência de 3 devs sobre o T13:** cada seção reclama o composite/canvas do seu jeito (T9: janela de posse; T12: scissor + restore). Sem uma regra única na amarração, dois renders por quadro ou "quem desenha por último vence". Além disso `domSync` chama `pointer.setCamera` no resize — F5 é imune (câmera própria), as outras que usam `pointer.ray` não são.
- **T11 (1ª)** dev OK — F4 `relevo/`: chapa gravada como espécime entre duas barras de carvão; normais do depth + ray march, luz no raio do cursor remapeada p/ o viewport da faixa, viagem em z dirigida pelo beat. Sem getBoundingClientRect por quadro, sem rAF novo, scissor no próprio retângulo. **`rayMarchSamples` → 8/4/0 no tier.ts** (decisão fechada): sombra mais longa = profundidade/tan(elevação) ≈ 0,04 un ≈ **29 px**; a marcha varre 0,06, logo 8 passos = 0,0075 ≈ **5 px**, a ordem da penumbra (4,3 px). Acima disso só reamostra platô. `low`=0 é o que a §3 F4 pede e segue sendo número, não caminho de código. 59,88 FPS (5 s de scroll, dpr 2). Inversão da luz medida: 59,6/31,8 → 24,6/33,1. V.4 provado: cursor fixo + só scroll, chapa 620×349→556×295 e a poça acompanha — luz em ponto fixo do mundo escorregaria.
- ✅ **Contrato do T13 convergiu sozinho (T11 + T12):** não existe clear global; região não desenhada compõe como fundo da página; toda seção que desenha usa scissor no próprio retângulo. Vira regra escrita no main.ts. T11 não chama `pointer.setCamera` (faz o próprio NDC→raio) — resolve metade do risco de estado global que o T12 apontou.
- **T16 (1ª)** dev OK — V.1 em produção: crânio CC BY 4.0 (martinjario) → **12.000 pontos** Int16 (V.5, sem decode) amostrados por área de face, atrás de casco decimado invisível (colorWrite:false + depthWrite, DoubleSide, **4,0% de encolhimento**). Build determinístico (sha256 idêntico em 2 runs). 59,9 FPS, reduced-motion byte a byte idêntico, 375×667 ok, 1 rAF.
  **Descarte medido: 52,8%** (24 poses), não os ~70% do catálogo — crânio é bem mais convexo que córtex, então fica perto do piso teórico de 50%. Diagnóstico que vale guardar: 19,6% dos pontos **virados para a câmera** também somem, e esse número não se move com o encolhimento entre 2,8% e 5,2% → é auto-oclusão real (dentes atrás do zigomático, fundo da órbita), não anatomia comida pelo casco.
  Duas decisões: enquadramento pela **esfera envolvente** com correção `asin(r/D)` (o objeto gira, a extensão vertical de agora é a horizontal de meia volta depois — enquadrar por eixo erraria em algum ponto); `uSize` **proporcional ao raio**, não fixo (fixo saturava 0,9% dos pixels no celular contra 0,1% no desktop; proporcional igualou em ~0,12%).
- ⚠️ **ORÇAMENTO LAZY NO LIMITE:** 195,6 (campo) + 312,2 (relevo) = **507,8 / 600 KB gzip**. Só se sustenta com three no **caminho crítico** (import estático → modulepreload). Se o T13 usar `import()` dinâmico, os ~125 KB migram para lazy e estoura em ~633 KB. Botão de escape: `--points=8000` (−55 KB), não trocar de técnica.
